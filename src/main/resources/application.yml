spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/SecurityAPI
    username: postgres
    password: DeusV-30
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
  env:
    jwtSecret: jwtGrokonezSecretKey
    jwtExpiration: 86400


